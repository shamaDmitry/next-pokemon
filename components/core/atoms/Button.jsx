"use client"

import classNames from "classnames";

const Button = () => {
  return (
    <button className={classNames(`p-3 rounded-lg bg-gray-700 text-white`)}>
      reset
    </button>
  );
}

export default Button;
